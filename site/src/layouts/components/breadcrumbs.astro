---
interface Props {
	pathname: string;
}
---

<div
	hx-select:inherited="main"
	hx-swap:inherited="outerHTML transition:true"
	hx-target:inherited="main"
	class="breadcrumbs"
	hx-boost:inherited="true"
>
	<ul class="justify-center">
		<li>
			<a href="/" style="view-transition-name: site-heading">bandori-stats</a>
		</li>
		{
			Astro.props.pathname
				.split("/")
				.filter(Boolean)
				.map((path, idx, parts) => (
					<li style={`view-transition-name: site-path-${path}`}>
						{idx === parts.length - 1 ? (
							path
						) : (
							<a
								href={
									idx > 0 ? `/${parts.slice(0, idx + 1).join("/")}` : `/${path}`
								}
							>
								{path}
							</a>
						)}
					</li>
				))
		}
	</ul>
</div>
