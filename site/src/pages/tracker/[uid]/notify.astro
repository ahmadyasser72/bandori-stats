---
import { stripBB } from "@bandori-stats/bestdori/helpers";
import { db } from "@bandori-stats/database";
import {
	getTrackingReference,
	TrackingTarget,
} from "@bandori-stats/database/tracker";
import SheetModal from "~/layouts/sheet-modal.astro";
import { NotifyWhenContainer } from "./_notify";

export const partial = true;

const uid = Astro.params.uid!;
const params = Astro.locals.parseQuery(TrackingTarget);

const snapshot = await db().query.trackerSnapshots.findFirst({
	columns: { uid: true, name: true, point: true, rank: true },
	where: { ...getTrackingReference(params), uid },
	orderBy: { id: "desc" },
});
---

<SheetModal class="mx-auto max-w-2xl">
	<h2 class="text-center text-2xl font-medium">
		{
			snapshot
				? `Notify me when ${stripBB(snapshot.name)}...`
				: "Notify me when..."
		}
	</h2>

	{snapshot && <NotifyWhenContainer client:load {snapshot} target={params} />}
</SheetModal>
