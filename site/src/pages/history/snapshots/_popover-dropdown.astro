---
interface Props {
	id: number;
	name: string;
	class?: string;
}

const { id, name, class: className } = Astro.props;
---

<div
	class:list={["dropdown dropdown-end dropdown-top", className]}
	id={`${name}-${id}`}
	popover
	style={`position-anchor: --anchor-${id}-actions`}
>
	<slot />
</div>
